{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the ClyTrade window. The app only uses core web APIs, so no plugin permissions are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
